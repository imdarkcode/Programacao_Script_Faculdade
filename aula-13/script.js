// $ = jQuery

$(document).ready(function(){ // Executar função quando o documento estiver pronto
    $('#btnCarregar').click(function() { // Executar função ao clicar no botão
        alert('Hello world');
    });
});